BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Paper] ADD [isOpen] BIT NOT NULL CONSTRAINT [Paper_isOpen_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
